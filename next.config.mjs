/** @type {import('next').NextConfig} */
const nextConfig = {
   
    images: {
      domains: ['admin.ingeniouslearn.com'], // Add the domain here
    },
  };
  
  export default nextConfig;
  